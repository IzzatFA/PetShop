# Rumah Hewan API

Backend API for Animal Adoption platform built with Express.js and Neon Postgres.

## Setup

1. Clone the repository
2. Run `npm install`
3. Configure `.env` with your `DATABASE_URL`
4. Run `npm start` (if nodemon is configured) to start the development server
